# دليل المفاتيح والتوكنز

كل المفاتيح تعيش في ملف `.env` على السيرفر فقط. **لا يوجد أي مفتاح في كود الواجهة** — المتصفح يكلّم `/api/chat` على سيرفرك، والسيرفر هو الوحيد الذي يكلّم Groq. لو حطّيت مفتاح Groq في JavaScript، أي زائر يقدر يفتح DevTools وياخده ويستهلك الحساب.

---

## 1. مفتاح Groq

**من فين:** [console.groq.com/keys](https://console.groq.com/keys) → سجّل دخول → `Create API Key` → انسخه فوراً (مش هيتعرض تاني).

**الشكل:** `gsk_` وبعدها سلسلة طويلة.

**في `.env`:**

```
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxx
GROQ_MODEL=llama-3.3-70b-versatile
```

**اختيار الموديل** — دي موديلات الإنتاج المتاحة على Groq وقت كتابة الملف:

| Model ID | السرعة تقريباً | السعر / مليون توكن | مناسب لإيه |
|---|---|---|---|
| `llama-3.3-70b-versatile` | ~280 t/s | $0.59 دخل / $0.79 خرج | **الافتراضي** — أفضل عربي في القائمة |
| `openai/gpt-oss-120b` | ~500 t/s | $0.15 / $0.60 | بديل أسرع وأرخص، جرّبه على أسئلة عربية قبل ما تعتمده |
| `llama-3.1-8b-instant` | ~560 t/s | $0.05 / $0.08 | رخيص جداً، جودته أقل في العربي — للتجارب فقط |

Groq بيغيّر القائمة دي بشكل دوري وبيوقف موديلات قديمة. اتأكد من المتاح حالياً بـ:

```bash
curl -s https://api.groq.com/openai/v1/models \
  -H "Authorization: Bearer $GROQ_API_KEY" | python3 -m json.tool
```

**الحدود:** خطة Developer فيها حد للتوكنز في الدقيقة (TPM) وللطلبات في الدقيقة (RPM). لو تجاوزت، الـ API بيرجّع `429`. المشروع فيه rate limit محلي (`RATE_LIMIT_PER_MIN`) يمنع مستخدم واحد من استهلاك الحصة.

**تقدير التكلفة:** كل سؤال بيستهلك تقريباً 1200 توكن دخل (السياق + البرومبت) و200 خرج. يعني على `llama-3.3-70b-versatile` حوالي **$0.0009 للسؤال** — ألف سؤال ≈ دولار واحد.

---

## 2. توكنز فيسبوك ماسنجر

محتاج ٣ قيم مختلفة، وكل واحدة ليها دور مختلف. ده اللي بيلخبط أغلب الناس.

| المتغير | إيه ده | من فين |
|---|---|---|
| `FB_VERIFY_TOKEN` | سلسلة **إنت بتخترعها**. ميتا بتبعتها لك مرة واحدة عند ربط الويبهوك عشان تتأكد إن السيرفر بتاعك | إنت تكتبها، وتحطها في نفس الخانة على الطرفين |
| `FB_PAGE_ACCESS_TOKEN` | التوكن اللي بيخلّي البوت يبعت رسائل **باسم الصفحة** | لوحة Meta → التطبيق → Messenger → Generate token |
| `FB_APP_SECRET` | سر التطبيق، بيتستخدم للتحقق من توقيع كل ويبهوك (`X-Hub-Signature-256`) | App Settings → Basic → App Secret |

### الخطوات بالترتيب

1. **صفحة فيسبوك للجامعة** — لازم تكون موجودة وإنت أدمن عليها.
2. [developers.facebook.com/apps](https://developers.facebook.com/apps) → `Create App` → نوع **Business**.
3. جوه التطبيق: `Add Product` → **Messenger** → `Setup`.
4. في قسم **Access Tokens**: اربط صفحة الجامعة، واضغط `Generate Token`. انسخه في `FB_PAGE_ACCESS_TOKEN`.
5. في **App Settings → Basic**: انسخ `App Secret` في `FB_APP_SECRET`.
6. اخترع سلسلة عشوائية وحطها في `FB_VERIFY_TOKEN` في ملف `.env`.
7. شغّل السيرفر على دومين **HTTPS** حقيقي (ميتا بترفض HTTP).
8. في Messenger → **Webhooks** → `Add Callback URL`:
   - Callback URL: `https://bot.anu.edu.eg/webhook`
   - Verify Token: نفس اللي في `.env` بالحرف
   - اضغط Verify and Save — لو ظهر خطأ، السيرفر مش شغال أو التوكن مختلف.
9. اضغط `Add Subscriptions` على الصفحة واختار: **messages** و **messaging_postbacks**.

### قبل ما تفتحه للناس

- التطبيق بيبدأ في وضع **Development**: البوت بيرد على الأدمنز والمختبرين بس. عشان يرد على أي حد، لازم **App Review** على صلاحية `pages_messaging` + **Business Verification** للجامعة (بتاخد أيام، ابدأ فيها بدري).
- **قاعدة الـ 24 ساعة:** تقدر ترد على المستخدم خلال ٢٤ ساعة من آخر رسالة منه. بعد كده الرسائل الترويجية ممنوعة.
- الويبهوك لازم يرجّع `200` في ثواني قليلة، عشان كده الرد بيتبعت في background task في `app/main.py`.

### اختبار محلي

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
# في تيرمنال تاني:
ngrok http 8000
# استخدم لينك ngrok كـ Callback URL
```

---

## 3. قواعد التعامل مع المفاتيح

- `.env` موجود في `.gitignore`. **لو رفعت مفتاح على GitHub بالغلط، اعتبره محروق** — امسحه من لوحة Groq/Meta واعمل واحد جديد. مسح الكوميت مش كفاية.
- على الاستضافة (Railway / Render / VPS) حط المتغيرات في لوحة الـ Environment Variables، مش في ملف مرفوع.
- اعمل مفتاح Groq منفصل للتجارب وواحد للإنتاج، عشان تقدر تلغي واحد من غير ما توقّف الخدمة.
- `ALLOWED_ORIGINS` لازم تتضبط على دومين الجامعة قبل الإطلاق، وإلا أي موقع يقدر يستخدم الـ API بتاعك.
